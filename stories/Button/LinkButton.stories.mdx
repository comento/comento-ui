import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import LinkButton, { linkButtonColors, linkButtonTargets } from "@/src/Components/Button/LinkButton";

<Meta
    title="General/Button/LinkButton"
    component={ LinkButton }
    argTypes={{
        label: {
            description: "버튼에 들어갈 텍스트",
            defaultValue: "링크 버튼",
            control: { type: "text" },
        },
        type: {
            description: "글자 크기",
            control: {
                type: "text",
            },
        },
        to: {
            description: "이동할 url",
            defaultValue: "javascript:void();",
            control: {
                type: "text",
            },
        },
        color: {
            description: "버튼 색상",
            control: {
                type: "select",
                options: linkButtonColors,
            },
        },
        external: {
            description: "외부 링크(a 태그 사용)",
        },
        nuxt: {
            description: "nuxt에서 사용",
        },
        target: {
            description: "문서가 열리는 위치",
            control: {
                type: "select",
                options: linkButtonTargets,
            },
        }
    }}
/>


export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { LinkButton },
    template:
        `<LinkButton
            :type="type"
            :to="to"
            :color="color"
            :external="external"
            :nuxt="nuxt"
            :target="target"
        >
            {{label}}
        </LinkButton>`,
});

# LinkButton

**LinkButton** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories
### All

<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
                <div>
                    <LinkButton>이동하지 않는 링크 버튼</LinkButton>
                    <LinkButton to="#" color="blue400" type="body2">맨위로 이동하기</LinkButton>
                    <LinkButton to="https://comento.kr" type="headline5">코멘토로 이동하기</LinkButton>
                </div>
        `,
            components: { LinkButton },
        }}
    </Story>
</Canvas>

### Colors
<Canvas>
    <Story name="Colors" height="100px">
        {{
            template: `
                <div>
                    <LinkButton color="blue400">blue400</LinkButton>
                    <LinkButton color="blue600">blue600</LinkButton>
                </div>
        `,
            components: { LinkButton },
        }}
    </Story>
</Canvas>
