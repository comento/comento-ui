import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Textarea, { textareaTypes } from "@/src/Components/DataEntry/Textarea";

<Meta
    title="Data Entry/Textarea"
    component={ Textarea }
    argTypes={{
        placeholder: {
            description: "Textarea placeholder",
            defaultValue: "추가 의견이 있으신가요?",
            control: { type: "text" },
        },
        value: {
            description: "Textarea 들어갈 텍스트",
            defaultValue: null,
            control: { type: "text" },
        },
        type: {
              description: "Textarea의 타입",
              control: {
                type: "select",
                options: textareaTypes,
              },
        },
        maxHeight: {
            description: "Textarea의 최대 높이",
            defaultValue: "auto",
            control: { type: "text" },
        },
        readOnly: {
            description: "Textarea readOnly 활성화",
            control: {
                type: "boolean",
            },
        },
    }}
/>


export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Textarea },
    template:
        `<div style="background:aliceblue; padding:20px">
            <Textarea :type="type"
                      :placeholder="placeholder"
                      :maxHeight="maxHeight"
                      :read-only="readOnly"
                      :value="value"/>
        </div>`,
});

# Textarea

**Textarea** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />
