import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Grid from "@/src/Components/Layout/Grid";
import Row from "@/src/Components/Layout/Row";
import StyleCol from "@/src/Components/Layout/StyleCol";
import InputGroup from '@/src/Components/DataEntry/InputGroup';
import TextField from '@/src/Components/DataEntry/TextField';
import Button from '@/src/Components/Button/Button';
import Box from '@/src/Components/DataDisplay/Box';
import Hint from '@/src/Components/DataDisplay/Hint';

<Meta
    title="Data Entry/InputGroup"
    component={ InputGroup }
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Box, InputGroup, TextField, Button },
    template:
        `<Box>
             <InputGroup>
                 <TextField placeholder="쿠폰 코드를 입력하세요" full />
                 <Button type="outlined" color="secondary">등록하기</Button>
             </InputGroup>
         </Box>`,
});

# InputGroup

**InputGroup** 컴포넌트의 문서입니다.

<Story name="Default" height="150px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

### Hint
<Canvas>
    <Story name="Hint" height="200px">
        {{
            template: `
                <Grid>
                    <Row class="mb-10">
                        <StyleCol :col-lg="4" col-gutters>
                            <InputGroup>
                                <TextField
                                    type="text"
                                    outlined
                                    placeholder="닉네임"
                                    full
                                    :value.sync="name"
                                />
                                <Button
                                    color="primary"
                                    type="outlined"
                                >
                                    중복검사
                                </Button>
                            </InputGroup>
                            <Hint>한글 10자, 영문∙숫자∙특수문자 20자까지 입력 가능합니다.<br v-show="isMobile">(혼용 가능)</Hint>
                        </StyleCol>
                    </Row>
                </Grid>
            `,
            data() {
                return {
                    name: null,
                };
            },
            components: { Grid, Row, StyleCol, InputGroup, TextField, Button, Hint },
        }}
    </Story>
</Canvas>
