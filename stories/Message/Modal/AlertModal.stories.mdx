import { ArgsTable, Meta, Story } from "@storybook/addon-docs/blocks";
import AlertModal from "@/src/Components/Message/Modal/AlertModal";
import Button from "@/src/Components/Button/Button";

<Meta
    title="Message/Modal/AlertModal"
    component={ AlertModal }
    argTypes={{
        show: {
            description: "모달 활성화",
            defaultValue: false,
            control: {
                type: "boolean",
            },
        },
        align: {
            description: "정렬",
            defaultValue: "center",
            control: {
                type: "select",
                options: ["center", "left", "right"]
            },
            table: {
                type: {
                    summary: "string",
                    detail: '"center" "left" "right"',
                },
            },
        },
        successMessage: {
            description: "버튼명",
            defaultValue: "확인",
            control: {
                type: "text",
            },
        },
        buttonColor: {
            description: "확인버튼 색상",
            defaultValue: "gray600",
            control: {
                type: "text"
            }
        },
        persistent: {
            description: "overlay 클릭해도 모달 닫지 않기",
            defaultValue: false,
            control: {
                type: "boolean",
            },
        },
        content: {
            description: "모달 내용",
            defaultValue: 'Alert modal<br/> 타이틀이 없고, 본문은 두 줄을 초과할 수 없다.',
            control: { type: "text" },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { AlertModal, Button },
    template: `
        <div>
            <Button @click="show = true">Open</Button>
            <AlertModal
                :show.sync="show"
                :align="align"
                :button-color="buttonColor"
                :persistent="persistent"
                :success-message="successMessage"
            >
                <template v-slot:content>
                    <div v-html="content" />
                </template>
            </AlertModal>
        </div>`
});

# AlertModal

**AlertModal** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />
