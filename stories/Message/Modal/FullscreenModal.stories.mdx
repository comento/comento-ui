import { ArgsTable, Meta, Story } from "@storybook/addon-docs/blocks";
import FullscreenModal, {fullScreenAlign} from "@/src/Components/Message/Modal/FullscreenModal";
import Button from "@/src/Components/Button/Button";
import Icon from '@/src/Elements/Core/Icon/Icon';

<Meta
    title="Message/Modal/FullscreenModal"
    component={ FullscreenModal }
    argTypes={{
        show: {
            description: "모달 활성화",
            control: {
                type: "boolean",
            },
        },
        align: {
            description: "모달 interaction 시작방향",
            defaultValue: "top",
            control: {
                type: "select",
                options: fullScreenAlign,
            },
        },
        showCloseButton: {
            description: "닫기 버튼 보이기",
            control: {
                type: "boolean",
            },
        },
        contents: {
            description: "모달 내용",
            defaultValue: "FullscreenModal",
            control: { type: "text" },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { FullscreenModal, Button },
    template: `
        <div>
            <Button @click="show = true">Open</Button>
            <FullscreenModal
                :show.sync="show"
                :show-close-button="showCloseButton"
                :align="align"
            >
                <template v-slot:header>
                    <p>Title</p>
                    <p>Sub</p>
                </template>
                <template v-slot:content>
                    <div style="padding: 18px 42px 18px 18px">
                        {{ contents }}
                    </div>
                </template>
            </FullscreenModal>
        </div>`
});

# FullscreenModal

**FullscreenModal** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />
