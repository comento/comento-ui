import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Tabs, { TabsTypes } from "@/src/Components/DataDisplay/Tabs";
import Button from "@/src/Components/Button/Button";
import Typography from "@/src/Elements/Core/Typography/Typography";
import Badge from "@/src/Components/DataDisplay/Badge";
import Chip from "@/src/Components/DataDisplay/Chip";

<Meta
    title="Data Display/Tabs"
    component={ Tabs }
    argTypes={{
        items: {
            description: "탭 아이템<br/>Array",
            defaultValue: ['Tab1', 'Tab2', 'Tab3', 'Tab4'],
            control: {
                type: "array",
            },
        },
        type: {
            description: "Swiper 사용 여부<br/>String",
            control: {
                type: "select",
                options: TabsTypes
            },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Tabs, Button },
    template:
      `<Tabs :type="type">
          <template v-for="(item, index) in 5" :slot="'item' + index">
                <Button>
                  Tab{{ item }}
                </Button>
          </template>
      </Tabs>`,
});

# Tabs

**Tabs** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories

### All
<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
            <div>
                <Tabs>
                    <template v-for="(item, index) in 5" :slot="'item' + index">
                            <Button>
                                Tab{{ item }}
                            </Button>
                    </template>
                </Tabs>
                <Typography type="caption1">basic type</Typography>
                <br/>
                <div>
                    <Tabs type="swiper">
                        <template v-for="(item, index) in 5" :slot="'item' + index">
                                <Button>
                                    Tab{{ item }}
                                </Button>
                        </template>
                    </Tabs>
                </div>
                <Typography type="caption1">swiper type</Typography>
                        <br/>
               <Tabs>
                   <template v-for="(item, index) in 5" :slot="'item' + index">
                           <Button style="position: relative">
                               Tab{{ item }}
                           </Button>
                          <Badge text="1" :offsetX="20" :offsetY="10"/>
                   </template>
               </Tabs>
            <br/>
            <Typography type="caption1">탭에 여러 컴포넌트가 들어가는 경우</Typography>
            <div>
                <Tabs type="swiper" :items="moreItems"/>
            </div>
            </div>
            `,
            components: { Tabs, Button, Chip, Badge },
        }}
    </Story>
</Canvas>