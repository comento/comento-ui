import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Tabs, { TabsTypes } from "@/src/Components/DataDisplay/Tabs";
import Typography from "@/src/Elements/Core/Typography/Typography";

<Meta
    title="Data Display/Tabs"
    component={ Tabs }
    argTypes={{
        items: {
            description: "탭 아이템<br/>Array",
            defaultValue: ['Tab1', 'Tab2', 'Tab3', 'Tab4'],
            control: {
                type: "array",
            },
        },
        type: {
            description: "Swiper 사용 여부<br/>String",
            control: {
                type: "select",
                options: TabsTypes
            },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Tabs, Typography },
    template:
      `<Tabs :items="items" :type="type"/>`,
});

# Tabs

**Tabs** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories
### Sizes
<Canvas>
    <Story name="Sizes" height="100px">
        {{
            data() {
                return {
                    items: ['Tab1','Tab2','Tab3','Tab4','Tab5','Tab6','Tab7','Tab8','Tab9','Tab10']
                }
            },
            template: `
                <div>
                    <Tabs type="swiper" :items="items"/>
                </div>
        `,
            components: { Tabs },
        }}
    </Story>
</Canvas>
