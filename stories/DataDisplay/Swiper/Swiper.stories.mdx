import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Swiper, {
    SwiperControlsColors,
    SwiperControlPositions,
    SwiperIndicatorColors,
    SwiperIndicatorPositions,
    SwiperSpacings,
} from "@/src/Components/DataDisplay/Swiper";
import Button from "@/src/Components/Button/Button";
import Typography from "@/src/Elements/Core/Typography/Typography";
import Box from "@/src/Components/DataDisplay/Box";
import SwiperItemExample from "./SwiperItemExample";

<Meta
    title="Data Display/Swiper"
    component={Swiper}
    argTypes={{
        withControls: {
            description: "With Controls",
            control: {
                type: "boolean",
            },
        },
        controlsPosition: {
            description: "Controls 위치",
            control: {
                type: "select",
                options: SwiperControlPositions,
            },
        },
        controlsColor: {
            description: "swiper control color",
            control: {
                type: "select",
                options: SwiperControlsColors,
            },
        },
        controlsCircle: {
            description: "동그라미모양 컨트롤",
            control: {
                type: "boolean",
            },
        },
        withIndicator: {
            description: "With Indicator",
            control: {
                type: "boolean",
            },
        },
        indicatorPosition: {
            description: "Indicator 위치",
            control: {
                type: "select",
                options: SwiperIndicatorPositions,
            },
        },
        indicatorColor: {
            description: "swiper indicator color",
            control: {
                type: "select",
                options: SwiperIndicatorColors,
            },
        },
        spacing: {
            description: "swiper indicator color",
            control: {
                type: "select",
                options: SwiperSpacings,
            },
        },
        slidesPerView: {
            description: "한번에 보여줄 슬라이드 개수",
            control: {
                type: "text",
            },
        },
        slidesPerGroup: {
            description: "그룹 안의 슬라이드 개수",
            control: {
                type: "number",
            },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Swiper, SwiperItemExample },
    template: `
        <div style="padding: 12px 32px;">
            <Swiper :with-controls="withControls" :controls-position="controlsPosition" :controls-color="controlsColor" :controls-circle="controlsCircle" :with-indicator="withIndicator" :indicator-position="indicatorPosition" :indicator-color="indicatorColor" :spacing="spacing" :slides-per-view="slidesPerView" :slides-per-group="slidesPerGroup">
                <template v-for="(item, index) in 10" :slot="'item' + index">
                <SwiperItemExample :text="'ITEM' + (index+1)" ></SwiperItemExample>
                </template>
            </Swiper>
        </div>
        `,
});

# Swiper

**Swiper** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories

### All

<Canvas>
    <Story name="All" height="100px">
        {{
            data() {
                return {
                    itemLength: 10,
                };
            },
            template: `
                    <div style="padding: 12px 24px;">
                    <Swiper controls-color="light" controls-position="inside" with-controls style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                    <Swiper controls-color="dark" controls-position="inside" with-controls style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                    <Swiper controls-color="light" controls-position="inside" with-controls controls-circle style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                    <Swiper controls-color="dark" controls-position="inside" with-controls controls-circle style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                     <Swiper controls-color="light" controls-position="outside" with-controls controls-circle style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                    <Swiper controls-color="dark" controls-position="top" with-controls style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                   <Swiper controls-color="light" controls-position="top" controls-circle with-controls style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                    </div>`,
            components: { Swiper, SwiperItemExample },
        }}
    </Story>
</Canvas>

### ControlsPosition

<Canvas>
    <Story name="ControlsPosition" height="100px">
        {{
            data() {
                return {
                    itemLength: 10,
                };
            },
            template: `
                <div style="padding: 12px 32px">
                    <Swiper controls-color="dark" controls-position="inside" with-controls style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                   <Swiper controls-color="dark" controls-position="outside" with-controls style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                    <Swiper controls-color="dark" controls-position="top" with-controls>
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                </div>`,
            components: { Swiper, SwiperItemExample },
        }}
    </Story>
</Canvas>

### ControlsColor

<Canvas>
    <Story name="ControlsColor" height="100px">
        {{
            data() {
                return {
                    itemLength: 10,
                };
            },
            template: `
                <div>
                    <Swiper controls-color="light" with-controls style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                   <Swiper controls-color="dark" with-controls>
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                </div>`,
            components: { Swiper, SwiperItemExample },
        }}
    </Story>
</Canvas>

### IndicatorColors

<Canvas>
    <Story name="IndicatorColors" height="100px">
        {{
            data() {
                return {
                    itemLength: 10,
                };
            },
            template: `
                <div>
                    <Swiper indicator-color="light" with-indicator style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                   <Swiper indicator-color="dark" with-indicator>
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                </div>`,
            components: { Swiper, SwiperItemExample },
        }}
    </Story>
</Canvas>

### IndicatorPosition

<Canvas>
    <Story name="IndicatorPosition">
        {{
            data() {
                return {
                    itemLength: 10,
                };
            },
            template: `
                <div style="padding: 24px 32px;">
                    <Swiper indicator-color="dark" indicator-position="inside" with-indicator style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                   <Swiper indicator-color="dark" indicator-position="outside" with-indicator>
                       <template v-for="(item, index) in itemLength" :slot="'item' + index">
                           <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                       </template>
                   </Swiper>
                </div>`,
            components: { Swiper, SwiperItemExample },
        }}
    </Story>
</Canvas>

### Autoplay

<Canvas>
    <Story name="Autoplay">
        {{
            data() {
                return {
                    itemLength: 10,
                };
            },
            template: `
                <div style="padding: 24px 32px;">
                    <Swiper controls-color="light" controls-position="inside" with-controls indicator-color="light" indicator-position="inside" with-indicator autoplay :slides-per-view="3" :slides-per-group="3" style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                           <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                   <Swiper controls-color="dark" controls-position="outside" with-controls indicator-color="dark" indicator-position="outside" with-indicator autoplay>
                       <template v-for="(item, index) in itemLength" :slot="'item' + index">
                          <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                       </template>
                   </Swiper>
                </div>`,
            components: { Swiper, SwiperItemExample },
        }}
    </Story>
</Canvas>

## Stories

### WithControls & WithIndicator

<Canvas>
    <Story name="WithControls & WithIndicator">
        {{
            data() {
                return {
                    itemLength: 10,
                };
            },
            template: `
                <div style="padding: 24px 32px;">
                    <Swiper controls-color="light" controls-position="inside" with-controls indicator-color="light" indicator-position="inside" with-indicator style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                           <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                   <Swiper controls-color="dark" controls-position="outside" with-controls indicator-color="dark" indicator-position="outside" with-indicator >
                       <template v-for="(item, index) in itemLength" :slot="'item' + index">
                          <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                       </template>
                   </Swiper>
                </div>`,
            components: { Swiper, SwiperItemExample },
        }}
    </Story>
</Canvas>

### sliderPerGroup

<Canvas>
    <Story name="sliderPerGroup">
        {{
            data() {
                return {
                    itemLength: 10,
                };
            },
            template: `
                <div style="padding: 24px 32px;">
                    <Swiper controls-color="dark" controls-position="inside" with-controls indicator-color="dark" indicator-position="inside" with-indicator :slides-per-view="3" :slides-per-group="3" autoplay style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                           <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                </div>`,
            components: { Swiper, SwiperItemExample },
        }}
    </Story>
</Canvas>

### slidesPerView auto

<Canvas>
    <Story name="slidesPerViewAuto">
        {{
            data() {
                return {
                    itemLength: 10,
                };
            },
            template: `
                <div style="padding: 24px 32px;">
                    <Swiper slides-per-view="auto" autoplay style="margin-bottom: 100px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                           <SwiperItemExample :text="'ITEM' + (index+1)"></SwiperItemExample>
                        </template>
                    </Swiper>
                </div>`,
            components: { Swiper, SwiperItemExample },
        }}
    </Story>
</Canvas>