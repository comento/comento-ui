import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Swiper, { SwiperTypes, SwiperIndicatorColors, SwiperControlsColors, SwiperSpacings, SwiperControlPositions } from '@/src/Components/DataDisplay/Swiper';
import Button from '@/src/Components/Button/Button';
import Typography from '@/src/Elements/Core/Typography/Typography';

<Meta
    title="Data Display/Swiper"
    component={ Swiper }
    argTypes={{
        type: {
            description: "swiper type",
            control: {
                type: "select",
                options: SwiperTypes,
            },
        },
        withControls: {
            description: "With Controls",
            control: {
                type: "boolean",
            },
        },
        controlsPosition: {
            description: "Controls 위치",
            control: {
                type: "select",
                options: SwiperControlPositions,
            },
        },
        controlsColor: {
            description: "swiper control color",
            control: {
                type: "select",
                options: SwiperControlsColors,
            },
        },
        withIndicators: {
            description: "With Indicators",
            control: {
                type: "boolean",
            },
        },
        indicatorColor: {
            description: "swiper indicator color",
            control: {
                type: "select",
                options: SwiperIndicatorColors,
            },
        },
        spacing: {
            description: "swiper indicator color",
            control: {
                type: "select",
                options: SwiperSpacings,
            },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Swiper, Typography },
    template: `
        <div style="padding: 12px 32px;">
            <Swiper :withControls="withControls" :controlsPosition="controlsPosition" :controlsColor="controlsColor" :withIndicators="withIndicators" :indicatorColor="indicatorColor" :spacing="spacing">
                <template v-for="(item, index) in 5" :slot="'item' + index">
                    <Typography type="display1" color="white">ITEM{{index+1}}</Typography>
                </template>
            </Swiper>
        </div>
        `,
});

# Swiper

**Swiper** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />


## Stories
### All
<Canvas>
    <Story name="All" height="100px">
        {{
            data() {
                return {
                    itemLength: 5,
                }
            },
            template: `
                    <div style="padding: 12px 24px;">
                    <Swiper :withControls="true" controlsColor="dark" indicatorColor="light">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <Typography type="display1" color="white">ITEM{{index+1}}</Typography>
                        </template>
                    </Swiper>
                    </div>`,
            components: { Swiper, Typography },
        }}
    </Story>
</Canvas>

## Stories
### IndicatorColors
<Canvas>
    <Story name="IndicatorColors" height="100px">
        {{
            data() {
                return {
                    itemLength: 5,
                }
            },
            template: `
                <div>
                    <Swiper indicatorColor="light" style="margin-bottom: 10px;">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <Typography type="display1" color="white">ITEM{{index+1}}</Typography>
                        </template>
                    </Swiper>
                   <Swiper indicatorColor="dark">
                        <template v-for="(item, index) in itemLength" :slot="'item' + index">
                            <Typography type="display1" color="white">ITEM{{index+1}}</Typography>
                        </template>
                    </Swiper>
                </div>`,
            components: { Swiper, Typography },
        }}
    </Story>
</Canvas>

