import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import COverlay from '@/components/elements/utility/COverlay.vue';
import CButton from '@/components/components/general/button/CButton.vue';

<Meta
  title="Utility/Overlay"
  component={COverlay}
    argTypes={{
        show: {
            description: "Overlay 활성화",
            control: {
                type: "boolean",
            },
        },
        type: {
          description: "Overlay 타입",
          control: {
            type: "select",
            options: ["dimmer"],
          },
        },
        persistent: {
            description: "overlay 클릭해도 닫지 않기",
            control: {
                type: "boolean",
            },
        },
        zIndex: {
            description: "zIndex",
            control: {
                type: "number",
            },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { COverlay, CButton },
  template: `
  <div>
        <CButton @click="show = true">Open</CButton>
        <COverlay
            :show.sync="show"
            :type="type"
            :persistent="persistent"
            :z-index="zIndex"
        />
    </div>`,
});

# Overlay

**c-overlay** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

