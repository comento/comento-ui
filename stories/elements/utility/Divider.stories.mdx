import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CDivider,  { dividerTypes } from '@/components/elements/utility/CDivider';
import { colorKeys } from '@/utils/constants/color';

<Meta
  title="Utility/Divider"
  component={CDivider}
    argTypes={{
        type: {
          description: "Divider의 타입",
          control: {
            type: "select",
            options: dividerTypes,
          },
        },
        vertical: {
          description: "Divider 수직, 수평 ",
          defaultValue: false,
          control: {
              type: "boolean",
          },
        },
        color: {
            description: "Divider 색상",
            defaultValue: "gray300",
            control: {
            type: "select",
            options: colorKeys,
          },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { CDivider },
  template: `
    <div class="mt-20" style="position:relative; height:300px; width:300px; background:lightcyan">
        <p style="position: absolute;
                  top: 50%;
                  transform: translateY(-50%);
                  width: 100%;
                  text-align: center;"
        >
            300 x 300 사이즈 기준
        </p>
        <CDivider :type="type" :vertical="vertical" :color="color" />
    </div>`,
});

# Divider

**Divider** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

