import { ArgsTable, Meta, Story, Props, Canvas } from '@storybook/addon-docs/blocks';
import EtcIcon, { EtcIconNames } from '@/components/elements/core/icon/EtcIcon.vue';
import { colorKeys } from '@/utils/constants/color';
import IconWrapper from './IconWrapper';
import CTypography from '@/components/elements/core/typography/CTypography.vue';

<Meta
    title="Core/Icon/EtcIcon"
    component={ EtcIcon }
    argTypes={{
        name: {
            description: "아이콘 이름",
            control: {
                type: "select",
                options: EtcIconNames,
            },
        },
        rotate: {
            description: "아이콘의 회전 각도",
            control: {
                type: "text",
            },
        },
        color: {
            description: "아이콘 색상",
            table: {
                type: {
                    summary: "목록",
                    detail: colorKeys.toString(),
                },
            },
            control: {
                type: "select",
                options: colorKeys,
            },
        }
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { EtcIcon },
    template: '<EtcIcon :name="name" :rotate="Number(rotate)" :color="color" />',
});

# EtcIcon

**EtcIcon** 컴포넌트의 문서입니다.<br />
디자인시스템에 추가하지 않고, 컴포넌트 내부에 사용되는 아이콘 (eg. Dropdown 화살표 아이콘 등)

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories
### All
<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
                <div>
                    <CTypography type="body1" color="gray500">아이콘을 클릭하면 클립보드에 아이콘 이름이 복사됩니다.</CTypography>
                    <!-- small -->
                    <div>
                        <CTypography type="body1" style="margin-bottom: 10px">Small</CTypography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <IconWrapper component="EtcIcon" name="IconDropdownSmallFillEtc" />
                        </div>
                    </div>
                    <!-- medium -->
                    <div>
                        <CTypography type="body1" style="margin-bottom: 10px">Medium</CTypography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <IconWrapper component="EtcIcon" name="IconDropdownMediumLineEtc" />
                        </div>
                    </div>
                </div>
            `,
             components: { IconWrapper, CTypography },
        }}
    </Story>
</Canvas>
