import { ArgsTable, Meta, Story, Props, Canvas } from '@storybook/addon-docs/blocks';
import AnimationIcon, { AnimationIconNames } from '@/src/components/elements/core/icon/AnimationIcon';
import IconWrapper from './IconWrapper';
import Typography from '@/src/components/elements/core/typography/Typography';

<Meta
    title="Core/Icon/AnimationIcon"
    component={ AnimationIcon }
    argTypes={{
        name: {
            description: "아이콘 이름",
            control: {
                type: "select",
                options: AnimationIconNames,
            },
        },
        rotate: {
            description: "아이콘의 회전 각도",
            control: {
                type: "text",
            },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { AnimationIcon },
    template: '<AnimationIcon :name="name" :rotate="Number(rotate)" />',
});

# AnimationIcon

**AnimationIcon** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories
### All
<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
                <div>
                    <Typography type="body1" color="gray500">아이콘을 클릭하면 클립보드에 아이콘 이름이 복사됩니다.</Typography>
                    <!--medium-->
                    <div>
                        <Typography type="body1" style="margin-bottom: 10px">Medium</Typography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <IconWrapper component="AnimationIcon" name="IconThreeDotMediumFillAnimation" :rotate="-90" />
                            <IconWrapper component="AnimationIcon" name="IconCheckRoundMediumLineAnimation" :rotate="90" />
                        </div>
                    </div>
                </div>
            `,
             components: { IconWrapper, Typography },
        }}
    </Story>
</Canvas>
