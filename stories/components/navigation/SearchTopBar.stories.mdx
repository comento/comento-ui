import { ArgsTable, Meta, Story } from '@storybook/addon-docs/blocks';
import CSearchTopBar from '@/components/components/navigation/CSearchTopBar.vue';

<Meta
  title="Navigation/SearchTopBar"
  component={CSearchTopBar}
  argTypes={{
    isTransparent: {
      description: "투명 헤더 사용 여부",
      table: {
        type: {
          summary: "Boolean",
        },
      },
    },
    keyword: {
      description: "검색어",
      defaultValue: "",
      control: { type: "string" }
    },
    placeholder: {
      description: "placeholder",
      defaultValue: "검색어를 입력 해주세요.",
      control: { type: "string" }
    },
    showDropdown: {
      description: "하단 드롭다운 flag",
      table: {
        type: {
          summary: "Boolean",
        },
      },
    },
  }}
/>

export const Default = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { CSearchTopBar },
  template: `
    <div style='width: 100%; display: flex; justify-content: center'>
    <div style='width: 375px'>
      <CSearchTopBar :is-transparent='isTransparent' :keyword='keyword' :placeholder='placeholder'
              :show-dropdown='showDropdown' :is-close="isClose" style="position: relative"  />
    </div>
    </div>
  `
});

# SearchTopBar

**c-search-top-bar** 컴포넌트의 문서입니다

<Story name="Default" height="100px">
	{Default.bind({})}
</Story>

<ArgsTable story="Default" />