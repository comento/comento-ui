import { ArgsTable, Meta, Story } from '@storybook/addon-docs/blocks';
import CTopBar,{ headerTypes } from '@/components/components/navigation/CTopBar.vue';
import CIconButton from '@/components/components/general/button/CIconButton.vue';

<Meta
  title="Navigation/TopBar"
  component={CTopBar}
  argTypes={{
    isTransparent: {
      description: '투명 헤더 사용 여부',
      table: {
        type: {
          summary: 'Boolean',
        }
      }
    },
    type: {
      description: '일반 혹은 로고 버전',
      control: {
        type: 'select',
        options: headerTypes,
      }
    },
	  title: {
		  description: '해당 페이지 타이틀',
		  defaultValue: '뒤로가기',
		  control: {
			  type: 'text',
		  }
	  }
  }}
/>

export const Default = (args, {argTypes}) => ({
  props: Object.keys(argTypes),
  components: {CTopBar, CIconButton},
  template: `
    <div style="width: 100%; display: flex; justify-content: center">
      <div style="width: 375px" :style="{backgroundColor : isTransparent ? 'black' : 'white' }">
        <CTopBar :is-transparent="isTransparent" :type="type" :title="title" style="position: relative" >
          <template v-slot:actions>
            <CIconButton icon-name="IconSearchLargeLine" size="medium" color="info" />
            <CIconButton icon-name="IconBookmarkLargeLine" size="medium" color="info" />
            <CIconButton icon-name="IconAlarmLargeLine" size="medium" color="info" />
          </template>
        </CTopBar>
  </div>
</div>`,
});


# TopBar

**c-top-bar** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
  {Default.bind({})}
</Story>

<ArgsTable story="Default" />