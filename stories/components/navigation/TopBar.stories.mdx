import { ArgsTable, Meta, Story } from '@storybook/addon-docs/blocks';
import TopBar from '@/components/components/navigation/TopBar';
import IconButton from '@/components/components/general/button/IconButton';

<Meta
  title="Navigation/TopBar"
  component={TopBar}
  argTypes={{
    isTransparent: {
      description: '투명 헤더 사용 여부',
      table: {
        type: {
          summary: 'Boolean',
        }
      }
    },
  }}
/>

export const Default = (args, {argTypes}) => ({
  props: Object.keys(argTypes),
  components: {TopBar, IconButton},
  template: `
    <div style="width: 100%; display: flex; justify-content: center">
      <div style="width: 375px">
        <TopBar :is-transparent="isTransparent" style="position: relative" >
          <template v-slot:actions>
            <IconButton icon-name="IconSearchLargeLine" size="medium" color="info" />
            <IconButton icon-name="IconBookmarkLargeLine" size="medium" color="info" />
            <IconButton icon-name="IconAlarmLargeLine" size="medium" color="info" />
          </template>
        </TopBar>
  </div>
</div>`,
});


# TopBar

**TopBar** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
  {Default.bind({})}
</Story>

<ArgsTable story="Default" />