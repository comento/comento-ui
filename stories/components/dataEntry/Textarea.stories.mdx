import { ArgsTable, Meta, Story } from '@storybook/addon-docs/blocks';
import CTextarea, { textareaTypes } from '@/components/components/dataEntry/CTextarea.vue';

<Meta
    title="Data Entry/Textarea"
    component={ CTextarea }
    argTypes={{
        placeholder: {
            description: "Textarea placeholder",
            defaultValue: "추가 의견이 있으신가요?",
            control: { type: "text" },
        },
        type: {
              description: "Textarea의 타입",
              control: {
                type: "select",
                options: textareaTypes,
              },
        },
        maxHeight: {
            description: "Textarea의 최대 높이",
            control: { type: "text" },
        },
        minHeight: {
            description: "Textarea의 최소 높이",
            control: { type: "text" },
        },
        readOnly: {
            description: "Textarea readOnly 활성화",
            control: {
                type: "boolean",
            },
        },
        error: {
            description: "에러 타입인지(outline type일 때만 가능)",
            control: {
                type: "boolean",
            },
        },
        hintMessage: {
            description: "에러 힌트 메시지",
            control: { type: "text" },
        },
    }}
/>


export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { CTextarea },
    data() {
        return {
            valueData: this.value,
        }
    },
    template:
        `<div style="background:aliceblue; padding:20px">
            <CTextarea :type="type"
                      :placeholder="placeholder"
                      :max-height="maxHeight"
                      :min-height="minHeight"
                      :read-only="readOnly"
                      :error="error"
                      :hint-message="hintMessage"
                      :value.sync="valueData"/>
        </div>`,
});

# Textarea

**c-textarea** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

### Error

<Canvas>
    <Story name="Align" height="200px">
        {{
            template: `
                <div style="width:300px">
                    <CTextarea
                        type="outline"
                        error
                    />
                </div>
            `,
            components: { CTextarea },
        }}
    </Story>
</Canvas>