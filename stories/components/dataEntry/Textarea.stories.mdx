import { ArgsTable, Meta, Story } from '@storybook/addon-docs/blocks';
import CTextarea, { textareaTypes } from '@/components/components/dataEntry/CTextarea';

<Meta
    title="Data Entry/Textarea"
    component={ CTextarea }
    argTypes={{
        placeholder: {
            description: "Textarea placeholder",
            defaultValue: "추가 의견이 있으신가요?",
            control: { type: "text" },
        },
        type: {
              description: "Textarea의 타입",
              control: {
                type: "select",
                options: textareaTypes,
              },
        },
        maxHeight: {
            description: "Textarea의 최대 높이",
            control: { type: "text" },
        },
        minHeight: {
            description: "Textarea의 최소 높이",
            control: { type: "text" },
        },
        readOnly: {
            description: "Textarea readOnly 활성화",
            control: {
                type: "boolean",
            },
        },
    }}
/>


export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { CTextarea },
    data() {
        return {
            value: '',
        }
    },
    template:
        `<div style="background:aliceblue; padding:20px">
            <CTextarea :type="type"
                      :placeholder="placeholder"
                      :max-height="maxHeight"
                      :min-height="minHeight"
                      :read-only="readOnly"
                      :value.sync="value"
            />
        </div>`,
});

# Textarea

**Textarea** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />
