import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Grid from '@/components/grid_legacy/Grid.vue';
import Row from '@/components/grid_legacy/Row.vue';
import StyleCol from '@/components/grid_legacy/StyleCol.vue';
import InputGroup from '@/components/input/InputGroup';
import TextField from '@/components/input/TextField';
import Button from '@/components/button/Button';
import Box from '@/components/box/Box.vue';
import SupportingText from '@/components/supportingText/SupportingText.vue';

<Meta
    title="Data Entry/Input/InputGroup"
    component={ InputGroup }
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Box, InputGroup, TextField, Button },
    template:
        `<Box>
             <InputGroup>
                 <TextField placeholder="쿠폰 코드를 입력하세요" full />
                 <Button type="outline" color="info">등록하기</Button>
             </InputGroup>
         </Box>`,
});

# InputGroup

**InputGroup** 컴포넌트의 문서입니다.

<Story name="Default" height="150px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

### Hint
<Canvas>
    <Story name="Hint" height="200px">
        {{
            template: `
                <Grid>
                    <Row class="mb-10">
                        <StyleCol :col-lg="4" col-gutters>
                            <InputGroup>
                                <TextField
                                    type="text"
                                    outline
                                    placeholder="닉네임"
                                    full
                                    :value.sync="name"
                                />
                                <Button
                                    color="primary"
                                    type="outline"
                                >
                                    중복검사
                                </Button>
                            </InputGroup>
                            <SupportingText>한글 10자, 영문∙숫자∙특수문자 20자까지 입력 가능합니다.<br v-show="isMobile">(혼용 가능)</SupportingText>
                        </StyleCol>
                    </Row>
                </Grid>
            `,
            data() {
                return {
                    name: null,
                };
            },
            components: { Grid, Row, StyleCol, InputGroup, TextField, Button, SupportingText },
        }}
    </Story>
</Canvas>
