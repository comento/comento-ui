import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CGrid from '@/components/layout/CGrid.vue';
import CRow from '@/components/layout/CRow.vue';
import CStyleCol from '@/components/layout/CStyleCol.vue';
import CInputGroup from '@/components/components/dataEntry/input/CInputGroup.vue';
import CTextField from '@/components/components/dataEntry/input/CTextField.vue';
import CButton from '@/components/components/general/button/CButton.vue';
import CBox from '@/components/components/general/CBox.vue';
import CHint from '@/components/components/dataDisplay/CHint.vue';

<Meta
    title="Data Entry/Input/InputGroup"
    component={ CInputGroup }
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { CBox, CInputGroup, CTextField, CButton },
    template:
        `<CBox>
             <CInputGroup>
                 <CTextField placeholder="쿠폰 코드를 입력하세요" full />
                 <CButton type="outline" color="info">등록하기</CButton>
             </CInputGroup>
         </CBox>`,
});

# InputGroup

**c-input-group** 컴포넌트의 문서입니다.

<Story name="Default" height="150px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

### Hint
<Canvas>
    <Story name="Hint" height="200px">
        {{
            template: `
                <CGrid>
                    <CRow class="mb-10">
                        <CStyleCol :col-lg="4" col-gutters>
                            <CInputGroup>
                                <CTextField
                                    type="text"
                                    outline
                                    placeholder="닉네임"
                                    full
                                    :value.sync="name"
                                />
                                <CButton
                                    color="primary"
                                    type="outline"
                                >
                                    중복검사
                                </CButton>
                            </CInputGroup>
                            <CHint>한글 10자, 영문∙숫자∙특수문자 20자까지 입력 가능합니다.<br v-show="isMobile">(혼용 가능)</CHint>
                        </CStyleCol>
                    </CRow>
                </CGrid>
            `,
            data() {
                return {
                    name: null,
                };
            },
            components: { CGrid, CRow, CStyleCol, CInputGroup, CTextField, CButton, CHint },
        }}
    </Story>
</Canvas>
