import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CFormGroup from '@/components/components/dataEntry/CFormGroup';
import CTextarea from '@/components/components/dataEntry/CTextarea';
import CTextField from '@/components/components/dataEntry/input/CTextField';

<Meta
    title="Data Entry/FormGroup"
    component={ CFormGroup }
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { CFormGroup, CTextField, CTextarea },
    template:
        `<div style="width:300px">
             <CFormGroup>
                 <CTextField outlined placeholder="사이 사이 간격 12px"/>
                 <CTextarea type="outlined" />
             </CFormGroup>
         </div>`,
});

# FormGroup

**FormGroup** 컴포넌트의 문서입니다.

<Story name="Default" height="150px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />
