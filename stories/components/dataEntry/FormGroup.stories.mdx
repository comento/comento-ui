import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CFormGroup from '@/components/components/dataEntry/FormGroup.vue';
import CTextarea from '@/components/components/dataEntry/Textarea.vue';
import CTextField from '@/components/components/dataEntry/input/TextField.vue';

<Meta
    title="Data Entry/FormGroup"
    component={ CFormGroup }
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { CFormGroup, CTextField, CTextarea },
    template:
        `<div style="width:300px">
             <CFormGroup>
                 <CTextField outline placeholder="사이 사이 간격 12px"/>
                 <CTextarea type="outline" />
             </CFormGroup>
         </div>`,
});

# FormGroup

**c-form-group** 컴포넌트의 문서입니다.

<Story name="Default" height="150px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />
