import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Banner from '@/components/banner/Banner.vue';
import Button from "@/components/button/Button";
import Typography from '@/components/typography/Typography';
import NewGrid from '@/components/grid/NewGrid.vue';
import NewRow from '@/components/grid/NewRow.vue';
import NewCol from '@/components/grid/NewCol.vue';

<Meta
	title="Message/Banner"
	component={Banner}
	argTypes={{
		type: {
			description: 'banner 종류',
			defaultValue: 'full',
			control: {
				type: 'select',
				options: ['standard', 'full'],
			},
		},
		title: {
			defaultValue: 'Title',
			control: {
				type: 'text',
			},
		},
		description: {
			defaultValue: 'Description',
			control: {
				type: 'text',
			},
		},
		alignItems: {
			defaultValue: 'center',
			control: {
				type: 'select',
				options: ['center', 'flex-start', 'flex-end'],
			},
		},
	}}
/>

export const Default = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { Banner, Button, Typography, NewGrid, NewRow, NewCol },
	template: `<div>
            <Banner :type="type" :title="title" :description="description" :align-items="alignItems">
                <template v-slot:background>
                    <div style="background-color: #f3f4f5; height: 100%;"></div>
                </template>
                <template v-slot:button v-if="type === 'full'">
                    <Button size="xlarge" full>CTA BTN</Button>
                </template>
            </Banner>
        </div>`,
});

# Banner

**Banner** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
	{Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories

### All

<Canvas>
	<Story name="All" height="500px">
		{{
			components: { Banner, Button, NewGrid, NewCol, NewRow, Typography },
			template: `<div>
                <Banner type="standard" title="Title" description="Description">
                <template v-slot:background>
                    <div style="background-color: #f3f4f5; height: 100%;"></div>
                </template>
            </Banner>
						<Banner class="mt-40" type="full" title="Title" description="Description">
                <template v-slot:background>
                    <div style="background-color: #f3f4f5; height: 100%;"></div>
                </template>
            </Banner>
            <Banner class="mt-40" type="full" title="Title" description="Description">
                <template v-slot:background>
                    <div style="background-color: #f3f4f5; height: 100%;"></div>
                </template>
                <template v-slot:button>
                    <Button size="xlarge" full>CTA BTN</Button>
                </template>
            </Banner>
            </div>`,
		}}
	</Story>
</Canvas>
