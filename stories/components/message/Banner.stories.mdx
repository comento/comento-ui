import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CBanner from '@/components/components/message/CBanner.vue';
import CButton from '@/components/components/general/button/Button.vue';
import CTypography from '@/components/elements/core/typography/Typography.vue';
import CNewGrid from '@/components/layout/NewGrid.vue';
import CNewRow from '@/components/layout/NewRow.vue';
import CNewCol from '@/components/layout/NewCol.vue';

<Meta
	title="Message/Banner"
	component={CBanner}
	argTypes={{
		type: {
			description: 'banner 종류',
			defaultValue: 'full',
			control: {
				type: 'select',
				options: ['standard', 'full'],
			},
		},
		title: {
			defaultValue: 'Title',
			control: {
				type: 'text',
			},
		},
		description: {
			defaultValue: 'Description',
			control: {
				type: 'text',
			},
		},
		alignItems: {
			defaultValue: 'center',
			control: {
				type: 'select',
				options: ['center', 'flex-start', 'flex-end'],
			},
		},
	}}
/>

export const Default = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { CBanner, CButton, CTypography, CNewGrid, CNewRow, CNewCol },
	template: `<div>
            <CBanner :type="type" :title="title" :description="description" :align-items="alignItems">
                <template v-slot:background>
                    <div style="background-color: #f3f4f5; height: 100%;"></div>
                </template>
                <template v-slot:button v-if="type === 'full'">
                    <CButton size="xlarge" full>CTA BTN</CButton>
                </template>
            </CBanner>
        </div>`,
});

# Banner

**c-banner** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
	{Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories

### All

<Canvas>
	<Story name="All" height="500px">
		{{
			components: { CBanner, CButton, CNewGrid, CNewCol, CNewRow, CTypography },
			template: `<div>
                <CBanner type="standard" title="Title" description="Description">
                <template v-slot:background>
                    <div style="background-color: #f3f4f5; height: 100%;"></div>
                </template>
            </CBanner>
						<CBanner class="mt-40" type="full" title="Title" description="Description">
                <template v-slot:background>
                    <div style="background-color: #f3f4f5; height: 100%;"></div>
                </template>
            </CBanner>
            <CBanner class="mt-40" type="full" title="Title" description="Description">
                <template v-slot:background>
                    <div style="background-color: #f3f4f5; height: 100%;"></div>
                </template>
                <template v-slot:button>
                    <CButton size="xlarge" full>CTA BTN</CButton>
                </template>
            </CBanner>
            </div>`,
		}}
	</Story>
</Canvas>
