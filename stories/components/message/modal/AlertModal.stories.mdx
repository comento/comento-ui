import { ArgsTable, Meta, Story } from '@storybook/addon-docs/blocks';
import CAlertModal from '@/components/components/message/modal/CAlertModal.vue';
import CButton from '@/components/components/general/button/CButton.vue';
import { colorKeys } from '@/utils/constants/color';

<Meta
    title="Message/Modal/AlertModal"
    component={ CAlertModal }
    argTypes={{
        show: {
            description: "모달 활성화",
            control: {
                type: "boolean",
            },
        },
        successMessage: {
            description: "버튼명",
            control: {
                type: "text",
            },
        },
        buttonColor: {
            description: "확인버튼 색상",
            control: {
                type: "select",
                options: colorKeys,
            }
        },
        persistent: {
            description: "overlay 클릭해도 모달 닫지 않기",
            control: {
                type: "boolean",
            },
        },
        content: {
            description: "모달 내용",
            defaultValue: 'Alert modal<br/> 타이틀이 없고, 본문은 두 줄을 초과할 수 없다.',
            control: { type: "text" },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { CAlertModal, CButton },
    template: `
        <div>
            <CButton @click="show = true">Open</CButton>
            <CAlertModal
                :show.sync="show"
                :button-color="buttonColor"
                :persistent="persistent"
                :success-message="successMessage"
            >
                <template v-slot:content>
                    <div v-html="content" />
                </template>
            </CAlertModal>
        </div>`
});

# AlertModal

**c-alert-modal** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />
