import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Toast, { toastTypes, toastPositions } from '@/components/components/message/Toast.vue';
import Button from '@/components/components/general/button/Button.vue';
import Icon, { IconNames } from '@/components/elements/core/icon/Icon.vue';

<Meta
    title="Message/Toast"
    component={ Toast }
    argTypes={{
        message: {
            description: "toast 메시지",
            defaultValue: "메시지가 들어가는 자리입니다.",
            control: {
                type: "text"
            },
        },
        show: {
            description: "활성화",
            control: {
                type: "boolean",
            },
        },
        type: {
            description: "종류",
            control: {
                type: "select",
                options: toastTypes,
            },
        },
        timeout: {
            description: "시간",
            control: {
                type: "number",
            },
        },
        position: {
            description: "방향",
            control: {
                type: "select",
                options: toastPositions,
            },
        },
        icon: {
            description: "toast 아이콘",
            control: {
                type: "select",
                options: IconNames,
            },
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Button, Toast },
    template: `
        <div>
            <Button @click="show = true">Open</Button>
            <Toast
                :show.sync="show"
                :type="type"
                :icon="icon"
                :message="message"
                :timeout="timeout"
                :position="position"
            />
        </div>`
});

# Toast

**Toast** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories
### With Icon
<Canvas>
    <Story name="WithIcon" height="100px">
        {{
            data: () => ({
                show: false,
            }),
            template: `
                <div>
                    <Button @click="show = true">Open</Button>
                    <Toast
                      :show.sync="show"
                      position="top"
                      type="success"
                      icon="IconSecurityLargeFill"
                      message="결제 정보 암호화 중"
                    />
                </div>
            `,
            components: {
                Toast, Button, Icon
            }
        }}
    </Story>
</Canvas>
