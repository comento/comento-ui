import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CLoader, { LoaderSizes } from '@/components/components/other/CLoader.vue';
import { buttonColors } from '@/components/components/general/button/CButton.vue';
import CTypography from '@/components/elements/core/typography/CTypography.vue';

<Meta
  title="Other/Loader"
  component={ CLoader }
  argTypes={{
      size: {
        description: "로더 크기<br/>`String`",
        table: {
          type: {
            summary: null,
          },
        },
        control: {
          type: "select",
          options: LoaderSizes,
        },
      },
      color: {
        description: "로더 색상<br/>`String`",
        table: {
          type: {
            summary: null,
          },
        },
        control: {
          type: "select",
          options: buttonColors,
        },
      },
      reversed: {
        description: "반전된 모양인지<br/>`Boolean`",
        defaultValue: false,
        table: {
          type: {
            summary: null,
          },
        },
      },
  }}
/>

export const Default = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { CLoader },
  template:
    '<CLoader :color="color" :size="size" :reversed="reversed"/>',
});

# Loader

**c-loader** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
  {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories

### All
<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
            <div>
                <div>
                    <!--small-->
                    <div>
                        <CTypography type="body1" style="margin-bottom: 10px">small</CTypography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <CLoader size="small" :color="item"></CLoader>
                            </template>
                        </div>
                    </div>
                    <!--medium-->
                    <div>
                        <CTypography type="body1" style="margin-bottom: 10px">medium</CTypography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                               <CLoader size="medium" :color="item"></CLoader>
                            </template>
                        </div>
                    </div>
                    <!--large-->
                    <div>
                        <CTypography type="body1" style="margin-bottom: 10px">large</CTypography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <CLoader size="large" :color="item"></CLoader>
                            </template>
                        </div>
                    </div>
                    <!--xlarge-->
                    <div>
                      <CTypography type="body1" style="margin-bottom: 10px">xlarge</CTypography>
                      <div style="display: flex; flex-wrap: wrap;">
                        <template v-for="(item, index) in buttonColors">
                          <CLoader size="xlarge" :color="item"></CLoader>
                        </template>
                      </div>
                    </div>
                </div>
                <div>
                    <!--small-->
                    <div>
                        <CTypography type="body1" style="margin-bottom: 10px">small</CTypography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <CLoader size="small" :color="item" reversed></CLoader>
                            </template>
                        </div>
                    </div>
                    <!--medium-->
                    <div>
                        <CTypography type="body1" style="margin-bottom: 10px">medium</CTypography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <CLoader size="medium" :color="item" reversed></CLoader>
                            </template>
                        </div>
                    </div>
                    <!--large-->
                    <div>
                        <CTypography type="body1" style="margin-bottom: 10px">large</CTypography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <CLoader size="large" :color="item" reversed></CLoader>
                            </template>
                        </div>
                    </div>
                    <!--xlarge-->
                    <div>
                        <CTypography type="body1" style="margin-bottom: 10px">xlarge</CTypography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <CLoader size="xlarge" :color="item" reversed></CLoader>
                            </template>
                      </div>
                    </div>
                </div>
            </div>
            `,
            data() {
                return {
                    buttonColors
                }
            },
            components: { CLoader, CTypography },
        }}
    </Story>
</Canvas>

### Sizes
<Canvas>
    <Story name="Sizes" height="100px">
        {{
            template: `
                <div style="display: flex; flex-wrap: wrap;">
                    <template v-for="item in LoaderSizes">
                        <CLoader :size="item" />
                    </template>
                </div>
            `,
            data() {
                return {
                    LoaderSizes,
                };
            },
            components: { CLoader },
        }}
    </Story>
</Canvas>

### Colors
<Canvas>
    <Story name="Colors" height="100px">
        {{
            template: `
                <div style="display: flex; flex-wrap: wrap;">
                    <template v-for="(item, index) in buttonColors">
                       <CLoader :color="item" />
                    </template>
                </div>
            `,
            data() {
                return {
                    buttonColors,
                };
            },
            components: { CLoader },
        }}
    </Story>
</Canvas>

### Reversed
<Canvas>
    <Story name="Reversed" height="100px">
        {{
            template: `
                <div style="display: flex; flex-wrap: wrap;">
                    <template v-for="(item, index) in buttonColors">
                       <CLoader :color="item" reversed />
                    </template>
                </div>
            `,
            data() {
                return {
                    buttonColors,
                };
            },
            components: { CLoader },
        }}
    </Story>
</Canvas>
