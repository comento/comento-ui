import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Loader, { LoaderSizes } from '@/components/components/other/Loader';
import { buttonColors } from '@/components/components/general/button/Button';
import Typography from '@/components/elements/core/typography/Typography';

<Meta
  title="Other/Loader"
  component={ Loader }
  argTypes={{
      size: {
        description: "로더 크기<br/>`String`",
        table: {
          type: {
            summary: null,
          },
        },
        control: {
          type: "select",
          options: LoaderSizes,
        },
      },
      color: {
        description: "로더 색상<br/>`String`",
        table: {
          type: {
            summary: null,
          },
        },
        control: {
          type: "select",
          options: buttonColors,
        },
      },
      reversed: {
        description: "반전된 모양인지<br/>`Boolean`",
        defaultValue: false,
        table: {
          type: {
            summary: null,
          },
        },
      },
  }}
/>

export const Default = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Loader },
  template:
    '<Loader :color="color" :size="size" :reversed="reversed"/>',
});

# Loader

**Loader** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
  {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories

### All
<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
            <div>
                <div>
                    <!--small-->
                    <div>
                        <Typography type="body1" style="margin-bottom: 10px">Small</Typography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <Loader size="small" :color="item"></Loader>
                            </template>
                        </div>
                    </div>
                    <!--medium-->
                    <div>
                        <Typography type="body1" style="margin-bottom: 10px">Medium</Typography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                               <Loader size="medium" :color="item"></Loader>
                            </template>
                        </div>
                    </div>
                    <!--large-->
                    <div>
                        <Typography type="body1" style="margin-bottom: 10px">Large</Typography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <Loader size="large" :color="item"></Loader>
                            </template>
                        </div>
                    </div>
                </div>
                <div>
                    <!--small-->
                    <div>
                        <Typography type="body1" style="margin-bottom: 10px">Small</Typography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <Loader size="small" :color="item" reversed></Loader>
                            </template>
                        </div>
                    </div>
                    <!--medium-->
                    <div>
                        <Typography type="body1" style="margin-bottom: 10px">Medium</Typography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <Loader size="medium" :color="item" reversed></Loader>
                            </template>
                        </div>
                    </div>
                    <!--large-->
                    <div>
                        <Typography type="body1" style="margin-bottom: 10px">Large</Typography>
                        <div style="display: flex; flex-wrap: wrap;">
                            <template v-for="(item, index) in buttonColors">
                                <Loader size="large" :color="item" reversed></Loader>
                            </template>
                        </div>
                    </div>
                </div>
            </div>
            `,
            data() {
                return {
                    buttonColors
                }
            },
            components: { Loader, Typography },
        }}
    </Story>
</Canvas>

### Sizes
<Canvas>
    <Story name="Sizes" height="100px">
        {{
            template: `
                <div style="display: flex; flex-wrap: wrap;">
                    <template v-for="item in LoaderSizes">
                        <Loader :size="item" />
                    </template>
                </div>
            `,
            data() {
                return {
                    LoaderSizes,
                };
            },
            components: { Loader },
        }}
    </Story>
</Canvas>

### Colors
<Canvas>
    <Story name="Colors" height="100px">
        {{
            template: `
                <div style="display: flex; flex-wrap: wrap;">
                    <template v-for="(item, index) in buttonColors">
                       <Loader :color="item" />
                    </template>
                </div>
            `,
            data() {
                return {
                    buttonColors,
                };
            },
            components: { Loader },
        }}
    </Story>
</Canvas>

### Reversed
<Canvas>
    <Story name="Reversed" height="100px">
        {{
            template: `
                <div style="display: flex; flex-wrap: wrap;">
                    <template v-for="(item, index) in buttonColors">
                       <Loader :color="item" reversed />
                    </template>
                </div>
            `,
            data() {
                return {
                    buttonColors,
                };
            },
            components: { Loader },
        }}
    </Story>
</Canvas>
