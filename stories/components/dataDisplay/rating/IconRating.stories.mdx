import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CIconRating from '@/components/components/dataDisplay/rating/CIconRating.vue';
import { colorKeys } from '@/utils/constants/color';
import { IconNames } from '@/components/elements/core/icon/CIcon.vue';

<Meta
	title="Data Display/Rating/IconRating"
	component={ CIconRating }
	argTypes={{
		color: {
			description: '색상',
			control: {
				type: 'select',
				options: colorKeys,
			},
		},
		iconName: {
			description: '아이콘 이름',
			control: {
                type: 'select',
                options: IconNames,
			},
		},
		text: {
			description: '내용',
            defaultValue: 3,
			control: {
				type: 'text',
			},
		},
	}}
/>

export const Default = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
    template: `
        <CIconRating :color="color" :text="text" :icon-name="iconName"/>
    `,
    components: { CIconRating },
});

# IconRating

**c-icon-rating** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
 {Default.bind({})}
</Story>

<ArgsTable story="Default" />

### All
<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
            <div style="display: flex; flex-direction: column">
                <CIconRating color="primary" text="6"/>
                <CIconRating color="error" icon-name="IconLikeSmallLine" text="6"/>
            </div>
    `,
            components: { CIconRating },
        }}
    </Story>
</Canvas>
