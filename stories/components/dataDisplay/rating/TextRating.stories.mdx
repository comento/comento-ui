import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CTextRating, {TextRatingSizes} from '@/components/components/dataDisplay/rating/CTextRating.vue';
import CTypography from '@/components/elements/core/typography/CTypography.vue';

<Meta
    title="Data Display/Rating/TextRating"
    component={ CTextRating }
    argTypes={{
        size: {
            description: '사이즈',
            control: {
                type: 'select',
                options: TextRatingSizes
            }
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    template: `
        <CTextRating :size="size">
            <template v-slot:text-left>
                댓글
            </template>
            <template v-slot:text-right>
              <strong>3</strong>
            </template>
        </CTextRating>
    `,
    components: { CTextRating },
});

# TextRating

**c-text-rating** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

### All
<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
            <div style="display: flex; flex-direction: column">
                <CTextRating size="default">
                    <template v-slot:text-left>
                        댓글
                    </template>
                    <template v-slot:text-right>
                        <CTypography color="gray850" :font-weight="500">
                            3
                        </CTypography>
                    </template>
                    </CTextRating>
                    <TextRating size="small">
                        <template v-slot:text-left>
                            총
                        </template>
                    <template v-slot:text-right>
                        <CTypography color="gray700" element="span" :font-weight="500">204</CTypography>건
                    </template>
                </TextRating>
            </div>
    `,
            components: { CTextRating, CTypography },
        }}
    </Story>
</Canvas>
