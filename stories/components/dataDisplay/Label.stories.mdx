import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Label from '@/src/components/components/dataDisplay/Label';

<Meta
    title="Data Display/Label"
    component={ Label }
    argTypes={{
        text: {
            description: "Label 들어갈 텍스트",
            defaultValue: 'Label',
            control: { type: "text" },
        },
        type: {
            description: "Label 타입",
            defaultValue: "dataEntry",
            control: {
                type: "select",
                options: ["dataEntry" ,"dataDisplay" ],
            },
        },
    }}
/>

export const Basic = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Label },
    template:
        `<Label :type="type" >{{text}}</Label>`,
});

# Label

**Label** 컴포넌트의 문서입니다.

<Story name="Basic" height="100px">
    {Basic.bind({})}
</Story>

<ArgsTable story="Basic" />


## Stories
### All
<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
                <div>
                 <div>
                    <Label type="dataEntry" >dataEntry Label</Label>
                 </div>
                 <div class="mt-4">
                    <Label type="dataDisplay" >dataDisplay Label</Label>
                 </div>
                </div>
        `,
            components: { Label },
        }}
    </Story>
</Canvas>

