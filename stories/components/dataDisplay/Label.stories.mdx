import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CLabel from '@/components/components/dataDisplay/CLabel';

<Meta
    title="Data Display/Label"
    component={ CLabel }
    argTypes={{
        text: {
            description: "Label 들어갈 텍스트",
            defaultValue: 'Label',
            control: { type: "text" },
        },
        type: {
            description: "Label 타입",
            defaultValue: "dataEntry",
            control: {
                type: "select",
                options: ["dataEntry" ,"dataDisplay" ],
            },
        },
    }}
/>

export const Basic = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { CLabel },
    template:
        `<CLabel :type="type" >{{text}}</CLabel>`,
});

# Label

**Label** 컴포넌트의 문서입니다.

<Story name="Basic" height="100px">
    {Basic.bind({})}
</Story>

<ArgsTable story="Basic" />


## Stories
### All
<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
                <div>
                 <div>
                    <CLabel type="dataEntry" >dataEntry Label</CLabel>
                 </div>
                 <div class="mt-4">
                    <CLabel type="dataDisplay" >dataDisplay Label</CLabel>
                 </div>
                </div>
        `,
            components: { CLabel },
        }}
    </Story>
</Canvas>

