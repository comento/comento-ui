import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CListItem from '@/components/components/dataDisplay/list/list/CListItem';
import CList from '@/components/components/dataDisplay/list/list/CList';
import CTypography from '@/components/elements/core/typography/CTypography';
import CDivider from '@/components/elements/utility/CDivider';
import CIcon from '@/components/elements/core/icon/CIcon';

<Meta
    title="Data Display/List/List/List"
    component={ CList }
    argTypes={{
        listItems: {
            description: "리스트 콘텐츠",
            defaultValue: ["List1", "List2", "List3", "List4", "List5"],
            control: {
                type: "array",
            },
        },
        spacing: {
            description: "내부 콘텐츠 사이의 여백",
            defaultValue: false,
            control: {
                type: "boolean",
            },
        },
        divider: {
            description: "구분선",
            defaultValue: false,
            control: {
                type: "boolean",
            },
        },
        size: {
            description: "ListItem 크기",
            defaultValue: "medium",
            table: {
                type: {
                    summary: "목록",
                    detail: '"small" "medium" "large"',
                },
            },
            control: {
                type: "select",
                options: ["small", "medium", "large"],
            },
        }
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { CList, CListItem, CTypography, CDivider },
    template:
        `<CList :spacing="spacing">
          <template v-for="(item, index) in listItems">
            <CListItem :key="'item-' + index" :size="size">
              <CTypography type="body2" color="gray800">
                {{ item }}
              </CTypography>
            </CListItem>
            <CDivider v-if="divider" :key="'divider-' + index" />
          </template>
        </CList>`,
});

# List

**List** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories
### All
<Canvas>
    <Story name="All" height="100px">
        {{
            data() {
                return {
                    menus: [
                        {
                            name: '멘토링 사례',
                            icon: 'IconMentoringMediumLine',
                        },
                        {
                            name: '직무소개',
                            icon: 'IconJobWikiMediumLine',
                        },
                        {
                            name: 'AI 자기소개서 분석기',
                            icon: 'IconAnalyticsMediumLine',
                        },
                    ],
                    activeIndex: 0,
                };
            },
            methods: {
                handleActiveColor(index) {
                    return this.activeIndex === index ? 'primary': 'gray600';
                },
            },
            template: `
                <div>
                    <CList>
                        <template v-for="(item, index) in menus">
                            <CListItem :key="'item-'+index" @click="activeIndex = index">
                                <CIcon :name="item.icon" :color="handleActiveColor(index)" style="margin-right: 4px" />
                                <CTypography type="body2" :color="handleActiveColor(index)">
                                    {{ item.name }}
                                </CTypography>
                            </CListItem>
                            <CDivider :key="'divider-' + index" />
                        </template>
                    </CList>
                </div>
        `,
            components: { CList, CListItem, CTypography, CDivider, CIcon },
        }}
    </Story>
</Canvas>
