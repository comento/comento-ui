import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Button from '@/src/components/components/general/button/Button';
import ButtonGroup, { buttonGroupSizes } from '@/src/components/components/general/button/ButtonGroup';
import Typography from '@/src/components/elements/core/typography/Typography';
import Icon from '@/src/components/elements/core/icon/Icon';

<Meta
  title="General/Button/ButtonGroup"
  component={ ButtonGroup }
  argTypes={{
    size: {
      description: "버튼의 크기",
      defaultValue: "small",
      control: {
        type: "select",
        options: buttonGroupSizes,
      },
    },
    vertical: {
      description: "방향",
      defaultValue: false,
      control: {
        type: "boolean",
      },
    },
  }}
/>

export const Default = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { ButtonGroup, Button },
  template:
    `<ButtonGroup :size="size" :vertical="vertical">
      <Button type="outlined" :size="size">Button</Button>
      <Button type="outlined" :size="size">Button</Button>
    </ButtonGroup>`,
});

# Button

**Button** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
  {Default.bind({})}
</Story>

<ArgsTable story="Default" />

### Sizes
<Canvas>
    <Story name="Sizes" height="100px">
        {{
            template: `
                <div>
                    <ButtonGroup size="small">
                        <Button type="outlined" size="small">Small</Button>
                        <Button type="outlined" size="small">Small</Button>
                    </ButtonGroup>
                    <ButtonGroup size="medium" style="margin: 10px 0">
                        <Button type="outlined" size="medium">Medium</Button>
                        <Button type="outlined" size="medium">Medium</Button>
                    </ButtonGroup>
                    <ButtonGroup size="large">
                        <Button type="outlined" size="large">Large</Button>
                        <Button type="outlined" size="large">Large</Button>
                    </ButtonGroup>
                </div>
        `,
            components: { ButtonGroup, Button },
        }}
    </Story>
</Canvas>

### Direction
<Canvas>
    <Story name="Direction" height="100px">
        {{
            template: `
                <div>
                    <Typography color="gray800">Horizontal ButtonGroup</Typography>
                    <ButtonGroup size="small" style="margin-bottom: 10px">
                        <Button size="small" type="text" color="secondary">
                            <template v-slot:left-icon>
                                <Icon name="IconPlusSmallLine" color="gray500" />
                            </template>
                            Button
                        </Button>
                        <Button size="small" type="text" color="secondary">
                            <template v-slot:left-icon>
                                <Icon name="IconPlusSmallLine" color="gray500" />
                            </template>
                            Button
                        </Button>
                    </ButtonGroup>
                    <Typography color="gray800">Vertical ButtonGroup</Typography>
                    <ButtonGroup size="small" vertical>
                        <Button type="outlined" size="small">Button</Button>
                        <Button type="outlined" size="small">Button</Button>
                    </ButtonGroup>
                </div>
        `,
            components: { ButtonGroup, Button, Typography, Icon },
        }}
    </Story>
</Canvas>
