import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { TypographyTypes } from '@/components/elements/core/typography/CTypography';
import CLinkButton, { linkButtonColors, linkButtonTargets } from '@/components/components/general/button/CLinkButton';

<Meta
    title="General/Button/LinkButton"
    component={ CLinkButton }
    argTypes={{
        label: {
            description: "버튼에 들어갈 텍스트",
            defaultValue: "링크 버튼",
            control: { type: "text" },
        },
        type: {
            description: "글자 크기",
            control: {
                type: "select",
                options: TypographyTypes,
            },
        },
        to: {
            description: "이동할 url",
            defaultValue: "javascript:void(0);",
            control: {
                type: "text",
            },
        },
        color: {
            description: "버튼 색상",
            control: {
                type: "select",
                options: linkButtonColors,
            },
        },
        external: {
            description: "외부 링크(a 태그 사용)",
        },
        nuxt: {
            description: "nuxt에서 사용",
        },
        target: {
            description: "문서가 열리는 위치",
            control: {
                type: "select",
                options: linkButtonTargets,
            },
        }
    }}
/>


export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { CLinkButton },
    template:
        `<CLinkButton
            :type="type"
            :to="to"
            :color="color"
            :external="external"
            :nuxt="nuxt"
            :target="target"
        >
            {{label}}
        </CLinkButton>`,
});

# LinkButton

**LinkButton** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories
### All

<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
                <div>
                    <CLinkButton>이동하지 않는 링크 버튼</CLinkButton>
                    <CLinkButton to="#" color="blue400" type="body2">맨위로 이동하기</CLinkButton>
                    <CLinkButton to="https://comento.kr" type="headline5">코멘토로 이동하기</CLinkButton>
                </div>
        `,
            components: { CLinkButton },
        }}
    </Story>
</Canvas>

### Colors
<Canvas>
    <Story name="Colors" height="100px">
        {{
            template: `
                <div>
                    <CLinkButton color="blue400">blue400</CLinkButton>
                    <CLinkButton color="blue600">blue600</CLinkButton>
                </div>
        `,
            components: { CLinkButton },
        }}
    </Story>
</Canvas>
