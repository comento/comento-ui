import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Typography from '@/components/elements/core/typography/Typography';
import IconButton, { IconButtonSizes } from "@/components/components/general/button/IconButton";
import { colorKeys } from '@/utils/constants/color';
import { IconNames } from "@/components/elements/core/icon/Icon";
import FAB, { FABColors } from "@/components/components/general/button/FAB";

<Meta
    title="General/Button/FAB"
    component={ FAB }
    argTypes={{
        iconName: {
            description: "버튼에 들어갈 아이콘",
            control: {
                type: "select",
                options: IconNames
            },
        },
        color: {
            description: "버튼의 색상",
            control: {
                type: "select",
                options: colorKeys,
            },
        },
        disabled: {
            description: "버튼의 disabled 상태",
        },
    }}
/>

export const Default = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { FAB },
    template:
        `<FAB
            :iconName="iconName"
            :color="color"
            :disabled="disabled"
        />`,
});

# FAB

**FAB** 컴포넌트의 문서입니다.

<Story name="Default" height="100px">
    {Default.bind({})}
</Story>

<ArgsTable story="Default" />

## Stories
### All
<Canvas>
    <Story name="All" height="100px">
        {{
            template: `
            <div class="flex">
              <div class="flex-column mr-30">
                <ButtonStateTitle>default</ButtonStateTitle>
                <template v-for="(color, index) in FABColors">
                  <FAB :color="color" class="mb-16"/>
                </template>
              </div>
              <div class="flex-column mr-30">
                <ButtonStateTitle>hover & active</ButtonStateTitle>
                <template v-for="(color, index) in FABColors">
                  <FAB :color="color" class="mb-16 hover-test"/>
                </template>
              </div>
              <div class="flex-column mr-30">
                <ButtonStateTitle>disabled</ButtonStateTitle>
                <template v-for="(color, index) in FABColors">
                  <FAB :color="color" disabled class="mb-16"/>
                </template>
              </div>
            </div>
            `,
            data() {
                return {
                    FABColors
                }
            },
            components: { FAB, Typography,
                'ButtonStateTitle': {
                    template: `<Typography type="body2" color="gray400" class="mb-10" style="min-width: 70px"><slot/></Typography>`,
                    components: {Typography},
                },
            },
        }}
    </Story>
</Canvas>